@model List<CQRSRentACar.Entities.Feature>
        <div class="container-fluid feature py-5">
            <div class="container py-5">
                <div class="text-center mx-auto pb-5 wow fadeInUp" data-wow-delay="0.1s" style="max-width: 800px;">
                    <h1 class="display-5 text-capitalize mb-3">Cental <span class="text-primary">Özellikler</span></h1>
                    <p class="mb-0">
                        Konforlu ve bakımlı araç filomuzla güvenli ve sorunsuz bir sürüş deneyimi sunuyoruz; anında online rezervasyon, şeffaf fiyatlandırma, temizlik ve bakım garantisi,sigorta paketleri dahil, 7/24 yol yardımı, esnek iptal ve değişiklik seçenekleri ile ekonomik ve farklı sınıflarda araçlar (şehir içi, SUV, minibüs vb.),isteğe bağlı ek sürücü ve kilometre seçenekleri sayesinde yolculuğunuz tamamen stressiz olsun.
                    </p>
                </div>
                <div class="row g-4 align-items-center">
                    <div class="col-xl-4">
                        <div class="row gy-4 gx-0">
                            @for (int i = 0; i < Model.Count && i < 2; i++)
                            {
                                <div class="col-12 wow fadeInUp" data-wow-delay="0.1s">
                                    <div class="feature-item">
                                        <div class="feature-icon">
                                            <span class="@Model[i].FeatureIcon fa-2x"></span>
                                        </div>
                                        <div class="ms-4">
                                            <h5 class="mb-3">@Model[i].FeatureTitle</h5>
                                            <p class="mb-0">@Model[i].FeatureDescription</p>
                                        </div>
                                    </div>
                                </div>
                            }
                        </div>
                    </div>
                    <div class="col-lg-12 col-xl-4 wow fadeInUp" data-wow-delay="0.2s">
                        <img src="/Cental-1.0.0/img/features-img.png" class="img-fluid w-100" style="object-fit: cover;" alt="Img">
                    </div>
                    <div class="col-xl-4">
                        <div class="row gy-4 gx-0">
                            @for (int i = 2; i < Model.Count && i < 4; i++)
                            {
                                <div class="col-12 wow fadeInUp" data-wow-delay="0.1s">
                                    <div class="feature-item justify-content-end">
                                        <div class="text-end me-4">
                                            <h5 class="mb-3">@Model[i].FeatureTitle</h5>
                                            <p class="mb-0">@Model[i].FeatureDescription</p>
                                        </div>
                                        <div class="feature-icon">
                                            <span class="@Model[i].FeatureIcon fa-2x"></span>
                                        </div>
                                    </div>
                                </div>
                            }
                        </div>
                    </div>
                </div>
            </div>
        </div>